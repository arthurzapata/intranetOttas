import{apiRequest}from'@/services/auth'
export interface Option{id:number;nombre:string;longitud?:number}export interface Cliente{id:number;tipo_documento_id:number;tipo_documento:string;documento:string;nombres:string;apellido_paterno?:string;apellido_materno?:string;nombre_completo:string;celular?:string;correo?:string;fecha_nacimiento:string;sexo:'m'|'f';predios_count:number}export interface Page{data:Cliente[];total:number;page:number;last_page:number;tipos_documento:Option[]}export interface Payload{tipo_documento_id:number;documento:string;nombres:string;apellido_paterno:string;apellido_materno:string;celular:string;correo:string;fecha_nacimiento:string;sexo:'m'|'f'}
const base='/lecturita/evan/catastro/clientes';export const listarClientes=(criterio:string,page=1)=>apiRequest<Page>(`${base}?criterio=${encodeURIComponent(criterio)}&page=${page}`);export const registrarCliente=(data:Payload)=>apiRequest<Cliente>(base,{method:'POST',body:JSON.stringify(data)});export const actualizarCliente=(id:number,data:Payload)=>apiRequest<Cliente>(`${base}/${id}`,{method:'PUT',body:JSON.stringify(data)});export const eliminarCliente=(id:number)=>apiRequest<void>(`${base}/${id}`,{method:'DELETE'})
