import{apiRequest}from'@/services/auth'
export interface Option{id:number;nombre:string}export interface Foto{id:number;url:string;principal:boolean;fecha?:string}export interface Conexion{id:number;codigo:string;sici:string;estado:string;situacion:string;categoria:string;servicios:string[]}export interface Propietario{id:number;nombre:string;documento_tipo?:string;documento?:string;telefono?:string}export interface Predio{id:number;distrito:string;sector:string;manzana:string;lote:string;sublote?:string;centro_poblado:string;urbanizacion:string;calle:string;direccion:string;referencia?:string;observacion?:string;propietario?:Propietario;foto_principal?:string;fotografias:Foto[];conexiones:Conexion[]}export interface Page{data:Predio[];total:number;page:number;last_page:number;distritos:Option[];sectores:Option[];manzanas:Option[];urbanizaciones:Option[];calles:Option[];centros_poblados:Option[]}export interface Payload{distrito_id:number;sector_id:number;manzana_id:number;lote:string;sublote:string;centro_poblado_id:number;urbanizacion_id:number;calle_id:number;direccion:string;referencia:string;observacion:string}
const base='/lecturita/evan/catastro/predios',qs=(v:Record<string,string|number>)=>{const p=new URLSearchParams();Object.entries(v).forEach(([k,x])=>x!==''&&p.set(k,String(x)));return p};export const listarPredios=(f:Record<string,string|number>)=>apiRequest<Page>(`${base}?${qs(f)}`);export const obtenerPredio=(id:number)=>apiRequest<Predio>(`${base}/${id}`);export const registrarPredio=(data:Payload)=>apiRequest<Predio>(base,{method:'POST',body:JSON.stringify(data)});export const buscarPropietarios=(criterio:string)=>apiRequest<Propietario[]>(`${base}/propietarios?criterio=${encodeURIComponent(criterio)}`);export const asignarPropietario=(predio:number,propietario:number)=>apiRequest<Predio>(`${base}/${predio}/propietario`,{method:'PUT',body:JSON.stringify({propietario_id:propietario})});export const agregarFoto=(predio:number,data:FormData)=>apiRequest<Foto>(`${base}/${predio}/fotografias`,{method:'POST',body:data});export const establecerPrincipal=(predio:number,foto:number)=>apiRequest<void>(`${base}/${predio}/fotografias/${foto}/principal`,{method:'PUT'});export const eliminarFoto=(predio:number,foto:number)=>apiRequest<void>(`${base}/${predio}/fotografias/${foto}`,{method:'DELETE'})
