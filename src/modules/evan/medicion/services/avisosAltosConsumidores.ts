import{apiRequest}from'@/services/auth'
export interface Distrito{id:number;nombre:string;entregados:number;total:number}export interface Aviso{id:number;cliente:string;direccion:string;distrito:string;medidor:string;inscripcion:string;suministro:string;categoria:string;lectura_anterior:number;lectura_actual:number;consumo:number;promedio:number;exceso_porcentaje:number;entregado:boolean;fecha_entrega?:string;entregado_por?:string;fotografia_predio?:string}export interface Page{data:Aviso[];total:number;page:number;last_page:number;distritos:Distrito[];resumen:{pendientes:number;entregados:number;total:number}}
const base='/lecturita/evan/medicion/avisos-altos-consumidores',qs=(v:Record<string,string|number>)=>{const p=new URLSearchParams();Object.entries(v).forEach(([k,x])=>x!==''&&p.set(k,String(x)));return p};export const listarAvisos=(f:Record<string,string|number>)=>apiRequest<Page>(`${base}?${qs(f)}`);export const entregarAviso=(id:number)=>apiRequest<Aviso>(`${base}/${id}/entregar`,{method:'POST'});export const entregarAvisos=(ids:number[])=>apiRequest<{actualizados:number}>(`${base}/entregar-masivo`,{method:'POST',body:JSON.stringify({ids})});export const urlAviso=(id:number)=>`${base}/${id}/imprimir`
